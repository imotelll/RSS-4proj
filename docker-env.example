# Variables d'environnement pour Docker Compose
# Copiez ce fichier vers .env si vous voulez personnaliser les valeurs

# Base de données
POSTGRES_USER=suprss
POSTGRES_PASSWORD=suprsspass
POSTGRES_DB=suprssdb

# Serveur
NODE_ENV=production
SESSION_SECRET=docker-session-secret-change-in-production
REPL_ID=docker-deployment
REPLIT_DOMAINS=localhost:4173,localhost:5000
ISSUER_URL=https://replit.com/oidc

# Ports (modifiez si nécessaire)
DB_PORT=5432
SERVER_PORT=5000
CLIENT_PORT=4173