# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=database_name

# Authentication Configuration
SESSION_SECRET=your-very-secure-session-secret-change-this-in-production
REPL_ID=your-repl-id-from-replit
REPLIT_DOMAINS=localhost:5000,your-custom-domain.com
ISSUER_URL=https://replit.com/oidc

# Frontend Configuration (for production builds)
VITE_API_URL=http://localhost:5000